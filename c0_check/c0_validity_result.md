# C0 조건 분포 재현 타당성 검증

## LGAI-EXAONE/EXAONE-3.5-2.4B-Instruct
- 문항 255개, 평균 Wasserstein거리 **0.8475**, 평균 선택지확률질량 0.9997
- subgroup r, 편차 기준 (성별×연령 10셀 평균): **-0.029** (범위 -0.264~0.217)
> C0는 페르소나가 없어 모델 예측이 셀마다 안 달라지므로, 이 r은 "모델이 인구통계 하위집단 차이를 우연히라도 맞히는지"를 재는 것이지 실제 조건화 능력을 재는 게 아니다. 이론상 0 근처가 정상(서울 논문 C0 실측 0.03) — 0에서 크게 벗어나면 편차 계산이나 셀 구성 자체를 재검토해야 한다는 뜻.

## kakaocorp/kanana-1.5-2.1b-instruct-2505
- 문항 255개, 평균 Wasserstein거리 **0.9230**, 평균 선택지확률질량 0.9954
- subgroup r, 편차 기준 (성별×연령 10셀 평균): **0.001** (범위 -0.199~0.215)
> C0는 페르소나가 없어 모델 예측이 셀마다 안 달라지므로, 이 r은 "모델이 인구통계 하위집단 차이를 우연히라도 맞히는지"를 재는 것이지 실제 조건화 능력을 재는 게 아니다. 이론상 0 근처가 정상(서울 논문 C0 실측 0.03) — 0에서 크게 벗어나면 편차 계산이나 셀 구성 자체를 재검토해야 한다는 뜻.
