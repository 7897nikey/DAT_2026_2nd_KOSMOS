# CLAUDE.md — KOSMOS 프로젝트 컨텍스트

> 이 파일을 프로젝트 루트에 두면 Claude Code가 자동으로 읽습니다.
> 또는 첫 대화에 통째로 붙여넣어 사용하세요.

---

## 0. 작업 규약

- **한국어로 응답한다.** 코드 주석과 문서도 한국어.
- **코드 수정 시 일회성 스니펫을 던지지 않는다.** 스크립트 전체 수정본을 파일로 제공하고, 어느 부분이 왜 바뀌었는지 매핑해 설명한다.
- **논리적 오류를 선제적으로 지적한다.** 검정 방향, 지표 선택, 통제 조건 누락 등.
- **확립된 사실과 가설을 구분해 서술한다.** 선행연구에서 검증되지 않은 주장을 사실처럼 쓰지 않는다.
- 제약 조건(데이터 접근성, 팀원 전공, 일정)을 먼저 확인한 뒤 옵션을 좁힌다.

---

## 1. 프로젝트

**한국어 실리콘 샘플링의 타당성 검증**
데이터분석 학회 8기 · 팀 KOSMOS · 김태우, 정주희(팀장), 허다교
정주희는 언어학 전공, 나머지 두 명은 비언어학 전공.

### 연구 질문

> LLM이 만든 응답 분포가 실제 분포와 다를 때, 그 차이는 무엇 때문인가?

관측되는 것은 "다르다"는 사실 하나이나 원인은 최소 네 가지이며, 선행연구는 이를 개별적으로 다루어 왔다. 본 프로젝트는 **동일한 자료와 실험 틀에서 네 성분을 분해**한다.

| 성분 | 어긋나는 지점 | 측정 | 개입 |
|---|---|---|---|
| ① 정보 부족 | 인구통계로 개인을 특정할 수 없음 | 셀 최빈응답 상한 대비 정확도 | 응답 이력 조건 추가 |
| ② 척도 표현 | 출력 분포가 최빈값에 집중 (모드 붕괴) | 출력 엔트로피, 셀 내 분산 재현율 | SSR, 디코딩 온도 |
| ③ 내용 편향 | 민감 문항에서 승인되는 답으로 이동 | 민감 문항 방향성 편차 | 인칭 × 화계 변형 |
| ④ 형식 민감도 | 문구·배열에 과민 반응 | TV거리, **인간 기준선 대비 배율** | 정순/역순 교차 |

**④가 고유 기여다.** 인간 기준선이 확보된 유일한 성분이라 "많다/적다"가 아니라 "인간의 몇 배"라고 말할 수 있다.

---

## 2. 자료

### 원본

- `2003-2025_KGSS_kor_public_v2.sav` (SPSS), `.dta` (Stata) — 동일 자료
- `2003-2025_KGSS_Codebook_v5.pdf` — 891쪽

**성균관대학교 서베이리서치센터(SRC)에서 별도 신청 절차 없이 받을 수 있다.**
KOSSDA 신청이 필요하다거나 재배포가 제한된다는 서술은 잘못된 것이므로 쓰지 말 것.

데이터 파일을 git에 커밋하지 않는 이유는 라이선스가 아니라 **용량과 재생성 가능성**이다.

### 기본 정보

| 항목 | 값 |
|---|---|
| 규모 | 23,282명 × 3,491변수, 17회차 |
| 회차 | 2003–2014 매년, 이후 격년, **2020 결번** |
| 회차별 표본 | 초기 1,300~1,600명 → 2016 이후 1,000~1,230명 |
| 2023년 표본 | 1,230명 |
| 모집단 | 전국 가구 거주 만 18세 이상 (시설 거주자·비한국어 화자 제외) |
| 조사 방식 | 조사원 방문 **대면면접**, 2023년 태블릿 99.0% |
| 표집 | 광역시도 층화 → 동/읍/면 200개 PPS → 집계구 → 가구 12~13개 → 가구원 1명 |

### 반드시 알아야 할 구조적 사실

**결측 코드는 두 개뿐이다.**

| 코드 | 의미 | 비율 |
|---|---|---|
| `-1` | 비해당 | 전체 셀의 **89.6%** |
| `-8` | 모름·무응답 | 0.2% |

`-1`에는 **두 상황이 혼재**한다.
- 해당 회차 미조사 → 정보 없음
- **건너뛰기 규칙에 의한 비해당** → 선행 문항 응답으로 확정된 값. 결측이 아니다.

구분법: 그 해에 `-1`이 아닌 값이 하나라도 있으면 조사된 문항이고, 그 안의 `-1`은 건너뛰기 비해당이다.

**값 라벨의 `8`, `88`, `9`, `99`는 통합 이전 회차의 잔여물이다.** 실제 출현하는 8과 9는 대부분 실질 응답이다(`ATTEND` 8 = "전혀 가지 않는다", `INCOME` 9 = "400만원~449만원"). 따라서 **코드값이 아니라 실질 선택지의 연속 구간(0 또는 1에서 시작해 1씩 증가)으로 판정**해야 한다.

**기타**

- `FINALWT`는 회차 내 정규화(전 회차 합/N = 1.0) → **연도 통합 가중 분석 불가**
- 사후층화 변수는 성별·나이·지역·도시성 → 우리 페르소나 변수와 동일. 가중 분포의 주변분포는 인구총조사와 일치
- 층·집락 변수 미공개 → **설계효과 반영 불가, 신뢰구간이 실제보다 좁게 추정됨**
- 응답자 식별자가 회차마다 재부여 → **반복 횡단면**. 개인 수준 분석은 단일 회차 내로 제한
- 2016년부터 **분할표본(split-ballot)** 설계 운영. `SAMPLEAB` 코드는 1/2 (코드북의 0/1 기재와 다름)
- 실제 조사에서 **"모르겠다"는 선택지로 제시되지 않았고** 자발적 응답만 기록됨
- **보기카드**로 선택지가 시각적으로 동시 제시됨

---

## 3. 확정된 설계 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 주 회차 | **2023** (오염 점검 결과에 따라 변경 가능) | 사실·태도·민감 문항이 모두 존재 |
| 보조 회차 | **2021** | 2023과 동일한 국가자긍심 문항에 A/B 분할표본 존재 |
| 분포 비교 단위 | **성별 × 연령** | 3변수 이상 교차 시 셀당 20명 미만 |
| 페르소나 변수 | 6개 전부 사용 | 프롬프트와 비교 단위를 분리 |
| 개인 예측 상한 | **셀 최빈응답 정확도** | η²는 분산 기반이라 명목형에 미정의, 정확도와 단위 상이 |
| η²의 지위 | **문항 선정의 층화 축**으로만 사용 | 재현도와의 관계는 미검증 (H3/H4로 강등) |
| "모르겠다" | 프롬프트에 선택지로 제공하지 않음 | 실제 조사에서 미제시 |
| 선택지 제시 | 전체를 한 번에 제시 | 보기카드 방식 재현 |
| 응답 형식 | **번호 응답 + prefill** | 아래 참조 |
| 원문자(①②③) | **배제** | 모델 간 토큰화 상이 |
| 분포 거리 지표 | Wasserstein 주, JS 병기, TV거리는 인간 기준선 대비에만 | KL은 비대칭·발산, 셋 모두 순서 무시 |

### 토큰화 실측 결과 (확정)

EXAONE 3.5와 Kanana 1.5에서 동일하게 확인:

- 숫자 1~9가 **모두 단일 토큰** (EXAONE 378~386, Kanana 16~24)
- **앞 공백이 별도 토큰으로 분리** (EXAONE 582, Kanana 220)
  → 프롬프트를 `"정답: "`처럼 공백으로 끝내 미리 소비하는 **prefill 필수**
- 한국어 척도 선택지는 **20개 중 19~20개가 다중 토큰** (평균 3.9~5.8)
  → 텍스트 생성 후 첫 토큰 판별 불가. **SSR 조건이 필요한 실증 근거**
- 원문자는 모델마다 처리가 다름 (Kanana는 ①②③만 단일, EXAONE은 전부 3토큰이며 디코딩 손상)

### 미결

- 오염 점검 결과에 따른 회차 확정
- 인칭 × 화계 수준 수 (3인칭 × 해라체의 수용성 검토 필요)
- RQ4(순서효과) 본실험 포함 여부 — 운영진 권고는 **포함**
- 상용 모델 유지 여부
- 파일럿 5문항의 본실험 포함 여부

---

## 4. 측정 결과

### 진단 (2023년 회차)

| 지표 | 값 |
|---|---|
| 조사된 변수 | 526개 / 3,491개 |
| 주 문항 후보 | **303개** (응답률 90%+, 비해당 5%-, 선택지 2~7개) |
| 조건부 문항 | **69개** |
| 셀 최빈응답 정확도 | **0.5385** (전체 최빈 0.5293) |
| 인구통계 순이익 | **중앙값 0**, 평균 0.0135, 303개 중 152개가 정확히 0, 최대 0.185 |
| η² 중앙값 | **0.018** |

셀 크기:

| 셀 구성 | 셀 수 | 최소 n | 중앙 n | n≥20 |
|---|---|---|---|---|
| 성별 × 연령 | 12 | 49 | 102 | 100% |
| + 학력 | 33 | 1 | 29 | 54.5% |
| + 도시성 | 57 | 1 | 9 | 35.1% |
| + 지역 | 155 | 1 | 5 | 9.7% |

### 인간 응답 순서 효과 (A/B 분할표본)

- A/B 쌍 **16개**, 그중 **13쌍이 선택지 배열 역전**
- TV거리 **0.02~0.05**
- 국가자긍심 10문항 중 **9문항 방향 일치** (우연 확률 약 1.1%)
- 방향은 뒤에 제시된 선택지 선호
- 주의: 낭독 + 보기카드 혼합 방식이라 최신/초두 효과 예측이 자명하지 않음. **경험적 관측**으로 서술할 것

### 파일럿 (팀원 실행, GPT-5.6 Luna)

- 2021년 profile 1,203명 대상 Direct + RSS, **18,045건, 오류 0**
- 최빈 쏠림: 인간 0.605 vs Direct 0.777 / RSS 0.813
- 유효 선택지 수: 인간 3.2 vs 모델 2.0~2.4
- **모델 연령 Cramér's V 0.328 vs 인간 0.087 (3.8배)**
- Direct vs RSS의 W1 거의 동일 (0.641 vs 0.640)
- **`top_logprobs` 상한으로 선택지 토큰 누락** → 오픈웨이트 전환의 실증 근거

### 전 회차 공통 문항

61개 중 실사용 가능 23개. **태도형은 기관 신뢰 배터리(`CON*` 16개)가 사실상 전부**이며 민감 문항이 없다. 따라서 시계열 트랙으로는 사회적 바람직성 편향을 검증할 수 없고, 재현도·오염 프로브 용도로만 쓴다.

---

## 5. 코드베이스

### 스크립트

| 파일 | 역할 |
|---|---|
| `kgss_inventory.py` | 결측 분해, 회차별 커버리지, 셀 크기, η², 셀 최빈응답 상한, 문항 후보 선정 |
| `kgss_codebook.py` | 코드북 PDF 파싱 → 설문 원문 추출, A/B 쌍 식별 및 순서효과 산출 |
| `kgss_export.py` | 코드북 xlsx, 회차별 라벨 CSV 변환 |
| `kgss_shortlist.py` | 3인 독립 태깅용 문항 선정 작업지 생성 |
| `kgss_code_audit.py` | 값 라벨 등재 코드와 실제 출현 코드 대조 (진단 도구) |
| `kgss_token_check.py` | 선택지 토큰화 및 첫 토큰 로짓 점검 |
| `kgss_contamination.py` | 사전학습 오염 프로브 생성 및 채점 |

### 산출물

```
inventory/
  kgss_clean.parquet      결측 치환 완료. 이후 모든 분석의 입력
  variables.csv           변수 사전 (라벨, 값라벨, 잔여 결측코드, ordinal_guess, battery)
  missing_by_year.csv     변수 × 연도별 iap_rate / dk_rate / answer_rate / in_wave
  year_coverage.csv       커버리지 행렬
  wave_items.csv          2023 문항 후보 (pass_answer, pass_iap 분리 기록)
  conditional_items.csv   건너뛰기 구조 문항
  common_items.csv        전 회차 공통 문항
  modal_baseline.csv      셀 최빈응답 정확도 (marginal_modal / cell_modal / gain)
  eta2.csv                문항별 인구통계 설명력
  cell_size_check.csv     셀 구성별 표본 크기
  diagnostics.md          요약

selection/
  variables_worded.csv    전 변수 설문 원문 + 선택지. 프롬프트 원천
  ab_pairs.csv            A/B 쌍과 인간 순서효과
  codebook_ab.md          요약
```

### 실행 순서

```powershell
uv run kgss_inventory.py --sav "2003-2025_KGSS_kor_public_v2.sav" --out .\inventory --target-year 2023
uv run kgss_codebook.py --pdf "2003-2025_KGSS_Codebook_v5.pdf" --inv .\inventory --out .\selection
```

### 환경

- Windows, PowerShell, **uv** (Python 3.12)
- 주요 패키지: `pyreadstat`, `pandas`, `pyarrow`, `pdfplumber`, `transformers`
- `pdftotext`(poppler)가 있으면 사용, 없으면 pdfplumber로 자동 전환
- 콘솔 한글 깨짐 방지: 스크립트마다 `sys.stdout`을 UTF-8로 감쌈

### .gitignore

데이터 파일 전체 제외. 단 `inventory/diagnostics.md`, `selection/codebook_ab.md`는 추적.
**주의**: `inventory/`가 아니라 `inventory/*`로 써야 부정 규칙(`!inventory/diagnostics.md`)이 작동한다.

---

## 6. 과거에 겪은 함정 — 반복하지 말 것

| 함정 | 증상 | 해법 |
|---|---|---|
| `-1`이 결측인 줄 몰랐음 | 모든 변수가 100% 응답으로 보임 | `-1`을 구조적 결측으로 마스킹 |
| `-8`을 구조적 결측으로 처리 | `--drop-dk` 옵션이 무력화됨 | `-8`은 DK로 분류. `--keep-dk`로 민감도 분석 |
| `IAP` 정규식 누락 | `inap`만 찾아 `IAP`를 놓침 | `\bIAP\b` 추가 |
| 라벨 어휘만으로 DK 판정 | `ELEFRAUDA`의 "있었는지 없었는지 모르겠다"가 무응답으로 오분류 | 실질 선택지 연속 구간 우선 |
| `EDUC`을 `pd.cut`으로 구간화 | 0=무학이 NaN, 8=서당한학과 88=DK가 대학원으로 분류 | 명시적 매핑 |
| `measure` 속성으로 순서형 판정 | 리커트 배터리가 nominal로 찍혀 464개만 잡힘 | 값 라벨 어휘로 추론 (1,718개) |
| A/B 문항에 일반 커버리지 기준 적용 | 응답률 50%라 전부 탈락 | `--ab-min-answer` 별도 기준 |
| 두 스크립트 간 열 이름 불일치 | `dk` → `legacy_dk` 변경 시 KeyError | `pick_col`로 버전 무관 처리 |
| 지역 변수 정규식에 "시도" 포함 | "자살 시도"가 오탐 | `REGION`, `URBAN` 명시 목록 |
| 한글 PDF 추출 도구 선택 | `-layout`은 문자 순서 파괴, PyMuPDF는 공백 소실 | `-raw` 또는 pdfplumber |
| 배터리 중복 | 파일럿 5문항 중 `CONBUS`·`CONGOVT`가 같은 기관 신뢰 배터리 | 배터리당 최대 1개 |
| Colab 환경 오염 | transformers 버전을 오르내리며 torch/torchvision 충돌 | 새 런타임에서 `-U transformers accelerate`만 설치 |

---

## 7. 현재 상태와 다음 단계

3주차 보고서 제출 완료. 운영진 피드백을 받은 상태.

### 운영진 지적 (요약)

- **Q1** Direct=RSS면 페르소나를 왜 주입하나 → 알고리즘적 충실도 불성립의 증거일 수 있음
- **Q2** 최빈 쏠림이 페르소나 때문인지 모델 성질인지 분리 불가 → **대조군 필요**
- **Q3** η² 0.018이면 "누구도 못 맞힌다" → **문항별 상한 대비 정규화** 필요
- **Q4** 오염 확인 시 대안 경로 부재
- **Q5** 선택지 토큰화 미확인
- **Q6** 문항 선정 기준 부재

### 실행 순서 (의존성 기준)

**1. 토큰화 점검** — 사실상 완료. 번호 응답 + prefill 확정. 로짓 누출률만 미측정.

**2. 오염 점검** — 진행 중. `kgss_contamination.py`로 프로브 생성 완료.
- B(원문 완성, 출처 명시/미명시 대조)가 핵심 지표
- **결정 게이트를 점검 전에 합의할 것**
- 대안 경로로 2025 전환을 검토하려면 2025 회차의 문항 유형 구성을 먼저 확인

**3. 기존 데이터 재분석** — 추가 호출 0건
- Q1 대응: RSS 프로필 중 **실제 데이터에 거의 없는 조합**(하위 10% 빈도)만 골라 Direct vs RSS 비교
  - 차이 없음 → 결합 구조 미사용 (지적이 맞음)
  - 차이 있음 → 결합을 쓰되 비현실 조합에서만 드러남
- 모델 연령 Cramér's V가 인간의 3.8배라는 점과 함께 읽으면, **"각 변수는 과잉 사용, 조합은 미사용"**이라는 해석이 가능
- Q3 대응: 파일럿 5문항의 셀 최빈 상한 산출 → LLM 정확도를 상한 대비로 재계산

**4. 실험 설계 확정**
- 대조군 4수준: 페르소나 없음 / "한국 성인" / 성별+연령 / 6변수
- 문항 30개 선정 (3인 태깅, 유형별 10개, 상한 구간 층화, 배터리당 1개)
- 인칭 × 화계 예문 수용성 검토

**5. 사전등록** → **6. 본 실험**

---

## 8. 모델 계획

| 역할 | 모델 | 비고 |
|---|---|---|
| 측정 대상 | EXAONE 3.5 (2.4B / 7.8B) | 원격 코드 방식이라 transformers 버전 의존 |
| | Kanana 1.5 (2.1B / 8B) | Apache 2.0. **config 검증 오류로 모델 로드 실패 중** |
| 비교 기준 | 다항 로지스틱, 랜덤포레스트 | 동일 인구통계로 학습 |
| 분석 도구 | 한국어 문장 임베딩 | SSR 사후 매핑 |

두 계열의 파라미터 규모가 대응(2.4B↔2.1B, 7.8B↔8B)되어 **크기와 계열을 직교 배치한 2×2 설계**가 가능하다.

**Kanana 로드 문제 — 해결됨 (2026-09-04, Colab에서 실행 확인)**: `config.json`은 hidden_size 1792, num_attention_heads 24, **head_dim 128을 명시**하고 있어 아키텍처 자체는 유효하다. 그런데 최신 transformers(5.x대, `huggingface_hub.dataclasses`의 `@strict` 검증)에 추가된 `LlamaConfig.validate_architecture()`가 `head_dim` 존재 여부와 무관하게 `hidden_size % num_attention_heads != 0`이면 무조건 예외를 던진다(`1792 % 24 = 16`). head_dim으로 우회 가능한 구성인데도 막는, 최신 transformers 쪽의 과도하게 엄격한 검사였다.
**해결책**: transformers를 최신(`-U`)이 아니라 Kanana `config.json`에 기록된 검증 버전 **`4.48.3`으로 고정**. `kgss_contam_colab.ipynb`에 반영. Colab에서 정상 로드 확인(첫 토큰 확률질량 0.9998~1.0, 오염 프로브 30건 완료).

**EXAONE 로드 문제 — 해결됨 (2026-09-04, Colab에서 실행 확인)**: `4.48.3`으로 고정하자 이번엔 EXAONE이 `ImportError: cannot import name 'RopeParameters' from transformers.modeling_rope_utils`로 실패했다. 원인은 EXAONE 저장소가 2026-02에 transformers v5 대응 커밋(`c38726a`, `ccce25b`)을 올려, `trust_remote_code=True`로 리비전을 안 박으면 그 최신 원격 코드를 받아오기 때문 — v5 API(`RopeParameters`)를 참조해 4.48.3과 안 맞는다.
**해결책**: `--revision 8e6fc27`(2024-12-09 최초 커밋, v5 대응 이전. `RopeParameters` 미참조 직접 확인함)로 고정. EXAONE과 Kanana는 필요한 `--revision`이 다르므로(Kanana 저장소엔 그 해시가 없음) **모델별로 따로 실행**한다 — `kgss_token_check.py`가 이번에 실행한 `--models`의 결과만 `logit_responses.csv`에서 교체하고 나머지는 보존하도록 고쳐서 순서와 무관하게 안전함. Colab에서 정상 로드 확인(첫 토큰 확률질량 0.983~1.0, 오염 프로브 30건 완료). 최종적으로 `logit_responses.csv` 60행(EXAONE 30 + Kanana 30) 확보.

**2×2 로짓 접근 실측 완료**: 이제 두 모델 모두 로짓 기반 오염 프로브(A)를 돌릴 수 있는 상태. 다음은 `kgss_contamination.py score`로 Wasserstein거리·대응표본 t검정 채점.

**환경 의존성 교훈**: 오픈웨이트의 재현 가능성은 가중치 고정만으로 확보되지 않는다. 라이브러리 버전과 원격 코드 리비전을 함께 고정해야 한다.

무료 자원(Kaggle 주 30시간, Colab)으로 2×2 실행이 가능한 규모다. 로짓 접근으로 반복 샘플링이 불필요해 연산량이 크게 준다.

---

## 9. 선행연구

| 문헌 | 내용 | 우리와의 관계 |
|---|---|---|
| Argyle et al. (2022/2023) | LLM을 조건부 확률 계산기로 봄. 알고리즘적 충실도 | 로짓 접근의 근거 |
| Aher et al. (2023) | 튜링 실험. 정렬 고도화가 인간 행동과의 괴리를 키울 수 있음 | 모델 크기 축의 근거 |
| Li & Conrad (2026) | SPIRIT. 인구통계 단독은 모드 붕괴 | 베이스라인 조건의 근거 |
| Heath & Alexander (2026) | SSR. 분포를 모르는 게 아니라 표집하지 못함. KL 1.97→0.07 | ② 성분. 2회차 시간적 일반화 검증 |
| Chapala et al. (2025) | 5조건 비교, 삼인칭 재구성만 효과. ANES 2020 vs 2024 2회차 검증 | ③ 성분. 요인 교차가 아니어서 분리 불가 |
| Sun et al. (2024) | Random Silicon Sampling. **harmlessness bias 명명** | RSS 조건, ③ 성분 |
| AHFE | 인구통계+맥락 결합이 우세. 모델 간·실행 간 편차 보고 | 모델 복수 사용의 근거 |

**주의**: 다회차 검증은 존재하나 파인튜닝 기반(Kim & Lee, GSS 1972–2021 retrodiction)이다. 제로샷 페르소나 프롬프팅으로는 2회차가 최대이며, 우리가 확장한다면 "처음 한다"가 아니라 **"2회차를 17회차로 확장한다"**로 서술해야 한다.

---

## 10. 서술 시 주의

- **"언어학 전공을 활용했다"고 강조하지 말 것.** 상대높임법이 인칭과 독립이라는 것은 학부 수준 지식이다. 기여는 **한국어의 유형론적 조건이 영어에서 불가능한 실험 조작을 허용한다**는 데 있다. "언어 유형적 조건"이라 쓸 것.
- 프레이밍은 형태론이 아니라 **화용론**(평가 우려, 자기제시, 공손성 전략)이 정확하다.
- η²와 재현도의 관계, 모드 붕괴의 원인은 **가설**이다. 사실처럼 쓰지 말 것.
- 인간 순서효과의 방향은 보기카드 병용 방식 때문에 이론적 예측이 자명하지 않다. **경험적 관측**으로 서술할 것.
- Chapala et al.의 조건이 요인 교차가 아니라는 주장은 우리 해석이지 논문의 진술이 아니다. "구조적으로 곤란하다"까지가 안전한 표현이다.
